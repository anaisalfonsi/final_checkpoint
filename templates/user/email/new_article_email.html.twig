{% apply inline_css(source('@email/email.css')) %}
<div class="logo">
    <img src="{{ email.image('@images/logo.png') }}" alt="Logo" width="100" height="100">
</div>
<h1>Hi <medium class="key-word">{{ user.name }}</medium>,</h1>

<h3>A new Article is out, check this out!</h3>

<h4>
    <a href="{{ path('article_show_one', {'id': article.id}) }}">{{ article.title }}</a>
</h4>

<h3>
    See yah! 🤡🤡🤡
</h3>
{% endapply %}
